/*
  Este ficheiro foi substituído pelo script consolidado:

  ** migrar_cadastro_cargo_completo_ensureDbCompat.sql **

  Execute esse ficheiro no SQL Server (ajuste USE). Contém renomeações, retiradas,
  seed MERGE, Líder/Lider e está alinhado ao ensureDbCompat do backend.
*/

RAISERROR('Use o script migrar_cadastro_cargo_completo_ensureDbCompat.sql (consolidado).', 10, 1);
GO

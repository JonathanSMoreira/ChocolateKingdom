# Copie para .env e ajuste o IP (ipconfig no Windows). Usado por app.config.js em dev e em `eas build` se o CLI carregar .env.
# APK na nuvem: defina a mesma variável em expo.dev → Project → Environment variables.
EXPO_PUBLIC_API_URL=http://192.168.15.3:3000
